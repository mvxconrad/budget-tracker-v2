# Production API base. Empty = same-origin: the frontend calls /api/... on
# whatever domain serves it (quarterbyte.net or the CloudFront URL), since
# CloudFront routes /api/* to the backend. Same-origin means no CORS, ever.
VITE_API_URL=
